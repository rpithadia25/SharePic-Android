<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="edu.sdsu.cs.sharepic.ProfileDetailActivity"
    android:orientation="vertical">

    <HorizontalScrollView android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:fillViewport="false"
        android:foregroundGravity="left"
        android:paddingBottom="1dp"
        android:paddingTop="1dp">
        <LinearLayout android:id="@+id/accounts_logo_container"
            android:layout_height="wrap_content"
            android:layout_width="wrap_content"
            android:orientation="horizontal"
            android:layout_gravity="center"
            android:scaleX="0.75"
            android:scaleY="0.75">
        </LinearLayout>
    </HorizontalScrollView>

    <Button
        android:id="@+id/images_preview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/pickImages"
        android:textColor="#000000"
        android:layout_gravity="center" />

    <HorizontalScrollView
        android:id="@+id/selected_images_scroll_view"
        android:layout_width="match_parent"
        android:layout_height="277dp"
        android:fillViewport="false"
        android:foregroundGravity="left"
        android:paddingBottom="1dp"
        android:paddingTop="1dp">

        <LinearLayout
            android:id="@+id/selected_images_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            android:animateLayoutChanges="true"
            android:gravity="start"
            android:orientation="horizontal"
            android:visibility="visible">

            <!-- images will be added dynamically to this layout -->
        </LinearLayout>
    </HorizontalScrollView>

</LinearLayout>
